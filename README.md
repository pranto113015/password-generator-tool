# Password Generator UI Tool

A simple password generator UI built with HTML, JavaScript, and Bootstrap. Features include:

- Password generation logic
- Password length slider
- Options for character types (uppercase, lowercase, numbers, symbols)
- Copy-to-clipboard button
- Real-time password generation

---